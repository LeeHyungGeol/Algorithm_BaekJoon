#include <iostream>
using namespace std;
int N;//색상환에 포함된 색의 개수,  N(4≤N≤1,000)
int K;//N색상환에서 선택할 색의 개수 K(1≤K≤N)
int dp[1001][1001] = { 0 };
int main() {

	cin >> N;
	cin >> K;
	
	for (int i = 0; i < N; ++i) {
		
	}

	return 0;
}