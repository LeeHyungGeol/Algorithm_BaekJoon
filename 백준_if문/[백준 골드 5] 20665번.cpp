#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

typedef pair<int, int> pii;

using namespace std;

int N, T, P;
string Start, End;
vector<pii> Times;
vector<vector<int>> Arr;

int main() {
	cin >> N >> T >> P;

	Arr.resize(N + 1, vector<int>(721, 0));

	for (int t = 0; t < T; ++t) {
		
	}

	return 0;
}

int timeToMinutes(string time) {
}